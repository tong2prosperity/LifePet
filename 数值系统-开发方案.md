# Pibo 数值系统 · 开发方案（MVP）

> 配套文件：`数值系统.xlsx`（所有可调旋钮与计算在内）
> 状态：✅ 可开发部分给出方案 / 🅿️ 暂缓待议 / 货币正式名 = **bo**（头顶绒毛，长一根 = 产生一 bo）
> 起草：2026-06 苏氨酸 × Claude 共创

---

## 0. 本文范围

本文只收**“可通过开发解决”**的问题，并给出落地方案。以下属于**产品/设计待议**，本轮暂缓，仅在 schema 里留口子：

- 🅿️ 养成系统的定义、"是否只有变才算养成"、"关系如何建立" —— 专题后议。
- 🅿️ **同步度 / 羁绊**（bo 买不到的第二条成长轨）—— 依赖上面的讨论，先在 schema 留 `rhythm_score` 等字段口子。
- 🅿️ 花品图鉴（问题5，TODO）、商店 / 饰品（问题4，留口子）。
- 🅿️ 第 6 点（待补）。

---

## 1. 问题清单与优先级

| # | 问题 | 能否开发解决 | 优先级 | 方案 |
|---|---|---|---|---|
| P0 | 事件账本 + 存储 schema | ✅ | **最高（现在不埋, 日后补不回）** | §3 |
| P0 | 规律奖励量化（问题2） | ✅ | 高 | §2 + xlsx「节律奖励」页 |
| P1 | 远程配置（可调参不发版） | ✅ | 高 | §4 |
| P1 | bo 产出 / 消耗 客户端实现 | ✅ | 高 | §5 |
| P1 | 拔毛枯萎 / 两轴隔离 | ✅ | 高 | §6 |
| P1 | 解锁 catalog + 前向口子（问题1/4） | ✅ | 中 | §7 |
| P2 | 情绪钩子物件（问题1） | ✅(机制) + 🎨(美术) | 中 | §8 |
| P2 | 周报（发放节律奖励） | ✅ | 中 | §2 + §3 |
| 🅿️ | 图鉴 / 商店 / 饰品 / 同步度 | 后续 | — | 留口子，§7 / §9 |

---

## 2. 规律奖励量化方案（问题2）

**原则**：奖励“跨天一致性”，不奖单日猛；**只加分不扣分**（不规律 = 拿不到 bonus，倍率永不 <1）；**宽容**（滚动 14 天窗 + 缓慢滚出，无连击清零）。依据：文献「睡眠规律性比时长更能预测死亡率」（见 xlsx「来源」页），世界观「Pibo 吃的是低熵 = 规律」。

### 2.1 节律分 RhythmScore（0–100，滚动 14 天）

```
RhythmScore = (睡眠节律R × 0.45 + 活动节律R × 0.30 + 出勤R × 0.25) × 100
```

| 子项 | 度量 | 映射(线性截断 0–1) |
|---|---|---|
| 睡眠节律 R_sleep | 睡眠中点时间的标准差 SD(min) | SD≤30→1，SD≥120→0 |
| 活动节律 R_act | 日步数变异系数 CV = SD/mean | CV≤0.25→1，CV≥0.75→0 |
| 出勤 R_show | 窗内“当日健康能量≥门槛(40)”的天数 ÷ 14 | 直接比例 |

> 公式：`R_sleep = MAX(0, MIN(1, (120−SD)/(120−30)))`；`R_act = MAX(0, MIN(1, (0.75−CV)/(0.75−0.25)))`；`R_show = 达标天/14`。全部断点为可调旋钮（xlsx）。MVP 用“睡眠中点 SD”简化版；进阶可换学界 **SRI（Sleep Regularity Index）**。

### 2.2 发放（两通道，均在当前 bo 经济内）

1. **bo 日倍率** = `1 + 0.5 × (RhythmScore/100)` → 区间 **1.00–1.50**，乘到「每日能量」算出的当日 bo（不规律只是没加成，**永不 <1**）。
2. **每周节律奖励** = `ROUND(8 × RhythmScore/100)` bo，在**周报**发放。

> 综合产出 ≈ `产出差异(每周) × bo日倍率 + 每周节律奖励`。

### 2.3 三档样例（已独立核算，见 xlsx「节律奖励」页，可调）

| 档案 | 睡眠中点SD | 步数CV | 窗内达标天 | 节律分 | bo日倍率 | 每周奖励 |
|---|---|---|---|---|---|---|
| 规律健康者 | 25 min | 0.20 | 13/14 | **98** | ×1.49 | 8 bo |
| 普通波动者 | 70 min | 0.50 | 8/14 | **54** | ×1.27 | 4 bo |
| 极不规律 | 130 min | 0.90 | 3/14 | **5** | ×1.03 | 0 bo |

### 2.4 实现要点 / 边界

- **数据依赖**：睡眠中点（HealthKit 睡眠段 / 现有 `sleepSegments`）、日步数序列（现有 `hourlySteps` / `HealthDayRecord`）、当日健康能量（已算）。三者均已在管线内。
- **数据不足**：窗内有效睡眠夜过少 → 睡眠节律按**中性 0.5**（不惩罚）；mean 步数 = 0 → 活动节律按中性。
- **反焦虑**：滚动窗 + 缓慢滚出，断一两天只小幅下降，**绝无“连击清零”**。
- **计算位置**：服务端按账本聚合 RhythmScore（也喂周报）；客户端可本地预算给即时反馈。

---

## 3. 事件账本 + 存储 Schema（P0，最高优先）

**为什么是 P0**：公式可以后改，**原始事件一旦没记就永久丢失**。账本支撑：回溯重算、周报聚合、防刷、分析。**这是唯一“现在不做、日后补不回来”的东西。**

### 3.1 三张核心结构

**`health_event`（原始事件，append-only，不可变）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | |
| user_id | uuid | |
| ts | timestamp | 事件发生时间 |
| type | enum | `steps_daily` / `sleep_segment` / `workout` / `photo` / `app_action` … |
| payload | json | 该类型的原始数据（步数、睡眠段起止+阶段、workout 明细…） |
| source | enum | `watch` / `phone` / `manual` |
| config_version | string | 当时生效的配置版本 |

**`bo_ledger`（bo 流水，append-only）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | |
| user_id | uuid | |
| ts | timestamp | |
| delta | int | +产出 / −消耗 |
| reason | enum | `earn_health` / `earn_app` / `earn_rhythm_weekly` / `spend_unlock` / `wither_decay` … |
| ref_id | uuid? | 关联事件 / 道具 |
| balance_after | int | 入账后余额（冗余, 便于审计） |
| config_version | string | |

**`economy_state`（派生快照，可由账本重建）**

`balance` / `unplucked_bo`（头顶未拔）/ `last_pluck_ts` / `rhythm_score` / `daily_source_counters`（各来源当日计数, 防刷封顶用）/ `unlock_flags`。

### 3.2 规则

- **append-only**；余额 = ledger 累加。
- **重算只向前**：改公式可重算未来产出，**永不倒扣已入账 bo / 已解锁道具**（守恒承诺）。
- 每条事件 / 流水**盖 `config_version`**，历史数据才解释得清。
- **服务端权威**；MVP 若来不及，可**本地先行但用同一 schema**，迁云无损。

---

## 4. 远程配置（P1）

把**所有数值旋钮**做成一个带 `version` 的配置：权重、评分曲线断点、`每个bo所需能量`、每日上限、道具价目、节律参数、数据源系数、衰减时长。

- 客户端启动**拉取 + 缓存**；每笔产出 stamp `config_version`。
- **调参 = 推配置，不发版**（避开鸿蒙商店审核延迟 —— 这是这版能“验证出结论”的前提）。
- 默认值 = `数值系统.xlsx` 当前旋钮。

---

## 5. bo 产出 / 消耗 客户端实现（接现有 iOS 架构，P1）

复用现有 `PetStateStore` + `HealthDataService.events` 管线：

```
原始 HealthKit 事件
  → 当日能量(配置曲线打分, 见 xlsx「评分曲线/每日能量」)
  → 能量池累计
  → 跨过“每个 bo 所需能量”阈值 → 生成 earn_health 事件 → +bo
```

- **乐观本地计算**给即时反馈（bo 实时涨 / 发芽动画），同步时与服务端真值**对账**。
- `bo` / `rhythm` 作为**额外派生 store**，持久化（SwiftData / UserDefaults）并镜像服务端。
- App 行为（拍照 / 小游戏 / 拍一拍彩蛋）走 `earn_app`，**按来源当日封顶**（防刷）。

---

## 6. 拔毛枯萎 / 两轴隔离（P1）

**两条互不干扰的轴**（务必在代码里物理隔离）：

| 轴 | 触发 | 影响 | 不影响 |
|---|---|---|---|
| **拔毛轴**(收没收) | 连续 3 天未拔 | `wither_decay`：仅清 `unplucked_bo`（头顶未拔的）+ 枯萎动画 | ❌ 不动 balance、不动已解锁道具 |
| **健康轴**(过没过好) | 健康数据持续差 | 信号变糊 → 潦草形态 + UI 降级（发疯/glitch 弧） | ❌ 与拔不拔毛无关 |

- 拔毛轴：追踪 `unplucked_bo` + `last_pluck_ts`，3 天定时器到 → 写 `wither_decay`（只作用于“未入账”）。
- 健康轴：独立状态机，由数据差驱动。
- 潦草态下 **Dashboard 历史 UI 保持不变**（已锁存记录，不随当前失锁损坏）。

---

## 7. 解锁 catalog + 前向兼容口子（问题1 / 问题4，P1）

**通用 catalog 模型**（不要把“线性”写死，否则商店化要返工）：

**`item`**：`id` / `name` / `type`(`furniture` / `accessory` / `flower` …) / `price_bo` 或 `unlock_source` / `unlock_rule`(`linear_index` / `shop` / `cognition`) / `effect`(可选, 见 §8) / `story_text`(卡片故事) / `art_ref`。

**`unlock_state`**：`user_id` / `item_id` / `unlocked_ts`。

- **MVP**：`type=furniture`，`unlock_rule=linear_index`（按累计 bo 阈值，3–5 件，价目见 xlsx「价目与TTU」）。
- **留口子（问题4）**：加 `type=accessory/flower` + `unlock_rule=shop/cognition` 即可扩展，**不动账本、不动货币**。
  - 商店 = catalog + rotation 配置；
  - 饰品 = 预留**穿戴槽位**字段；
  - 图鉴（问题5）= `collection` 表（花品，`unlock_source = 认知能量/拍照`，**不用 bo 买**）。

---

## 8. 情绪钩子物件（问题1，P2）

**开发侧**：给 `item.effect` 一个可选字段 + 一组 effect 类型，让物件不只是贴图：`change_scene` / `trigger_pibo_behavior` / `change_background` / `drop_story_fragment`。

**候选物件 + 钩子**（供与 UI 商量）：

| 物件 | 情绪钩子 | effect 类型 |
|---|---|---|
| 旧台灯 | 点亮 → 深眠背景多一盏暖灯，Pibo 睡前看一眼 | change_scene |
| 软垫 | 放置 → 发呆态 Pibo 有概率过去打盹 | trigger_pibo_behavior |
| 窗 | 白天背景透光 / 晨雾（随初醒态） | change_background |
| 旧收音机 | 偶尔漏一句“信号”碎片（接故事线） | drop_story_fragment |
| 空花盆 | 为花品图鉴留位，花开时有处可栽 | (口子) |

**美术怎么出**（回答“你有什么办法绘制”）：我可以提供三选一 ——
(a) **概念 SVG 草图**（我直接画，给灵感）；(b) **可视化 mockup**（带样式预览）；(c) **给 wowo 的精确美术 spec**（尺寸 / 风格 / 状态）。建议：**spec 为主 + 按需草图**，正式素材交 wowo。

---

## 9. 落地顺序建议

1. **P0：事件账本 + 埋点 schema**（本周，卡 7/12 埋点节点）。
2. 远程配置骨架。
3. bo 产出/消耗 + 拔毛枯萎，接 `PetStateStore`。
4. 规律奖励（节律分）+ 周报。
5. 解锁 catalog（留好口子）。
6. 情绪钩子 effect + 美术。

---

## 10. 暂缓 / 待议（不在本轮开发范围）

- 🅿️ 养成与“关系”的定义专题（含“是否只有变才算养成”）。
- 🅿️ 同步度 / 羁绊第二轨（buy 不到的成长轨）—— schema 先留 `rhythm_score` 等口子。
- 🅿️ 花品图鉴（问题5）、商店 / 饰品（问题4）—— 已在 §7 留口子。
- 🅿️ 第 6 点 —— 待补。
