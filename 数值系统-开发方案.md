# bo 事件账本设计参考

> 历史结构参考，非现行开发指令。当前计算、日界、奖励和消费规则由已发布 Core 负责；本地优先与合并见决定 031，能力与成本见 046。旧节律奖励、远程旋钮、拔取枯萎及路线图已移除。

## 3. 事件账本 + 存储 Schema（P0，最高优先）

**为什么是 P0**：公式可以后改，**原始事件一旦没记就永久丢失**。账本支撑：回溯重算、周报聚合、防刷、分析。**这是唯一“现在不做、日后补不回来”的东西。**

### 3.1 三张核心结构

**`health_event`（原始事件，append-only，不可变）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | |
| user_id | uuid | |
| ts | timestamp | 事件发生时间 |
| type | enum | `steps_daily` / `sleep_segment` / `workout` / `photo` / `app_action` … |
| payload | json | 该类型的原始数据（步数、睡眠段起止+阶段、workout 明细…） |
| source | enum | `watch` / `phone` / `manual` |
| config_version | string | 当时生效的配置版本 |

**`bo_ledger`（bo 流水，append-only）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | |
| user_id | uuid | |
| ts | timestamp | |
| delta | int | +产出 / −消耗 |
| reason | enum | `earn_health` / `earn_app` / `earn_rhythm_weekly` / `spend_unlock` / `wither_decay` … |
| ref_id | uuid? | 关联事件 / 道具 |
| balance_after | int | 入账后余额（冗余, 便于审计） |
| config_version | string | |

**`economy_state`（派生快照，可由账本重建）**

`balance` / `unplucked_bo`（头顶未拔）/ `last_pluck_ts` / `rhythm_score` / `daily_source_counters`（各来源当日计数, 防刷封顶用）/ `unlock_flags`。

### 3.2 规则

- **append-only**；余额 = ledger 累加。
- **重算只向前**：改公式可重算未来产出，**永不倒扣已入账 bo / 已解锁道具**（守恒承诺）。
- 每条事件 / 流水**盖 `config_version`**，历史数据才解释得清。
- 当前由已发布 Core 提供规则，本地优先形成 bo，登录后幂等合并；旧 schema 只用于理解迁移背景。

---
